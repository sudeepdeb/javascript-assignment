"Use strict"

function countCharacter(s) {
    var userString = new String(s);
    console.log(userString);
    var userStringLength = userString.length;
    console.log(userStringLength);
}

countCharacter("This is Javascript");